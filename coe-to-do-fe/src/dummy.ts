// src/dummy.ts
import { gql } from './__generated__'

// A placeholder query matching your Rails schema to satisfy codegen's search engine
const DUMMY_QUERY = gql(`
  query GetTodosPlaceholder {
    allLists {
      id
    }
  }
`)